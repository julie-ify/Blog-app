<div>
  <div>
    <%= link_to user_post_path(@user.id, post.id), id: "user_post#{post.id}" do %>
      <h5 class="post__title"><%= post.title %></h5>
    <% end %>
  <p><%= post.text %> </p>
  <div>
<span>Comments: <%= post.comments.size %></span>
<span>Likes: <%= post.comments.size %></span>
  </div>
</div>