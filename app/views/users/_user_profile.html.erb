<section>
  <div class="d-flex">
    <img src=<%= user_img(user) %> alt="User profile picture" >
    <div class="user_list">
      <h2 class="user_name"><%= user.name %></h2>
      <p class="num_post">Number of posts: <%= user.posts.size %></p>
    </div>
  </div>
</section>